# Delivery Log

- Plan v1 / P1 started: admitted a bounded offline consultation-lane package; source state was clean at the required starting commit. Next state: PRECODE review.
- Evidence: phase validator reported `PRECODE PASS` before production edits.
- Plan v1 / P2 evidence: the inherited checkpoint recorded strict RED→GREEN for durable/idempotent local ask, MCP ask→claim→answer→read, and the negative family; closure review found the implementation confined to the accepted consultation lane.
- Evidence: `python3 -m pytest -q tests/test_consultation.py tests/test_queue.py tests/test_mcp.py` passed 35 tests; `python3 -m pytest -q` passed the full 36-test suite.
- Plan v1 / P3 evidence: the README first screen distinguishes a quick question from a formal task with a weekly-update example, while retaining installation, loopback/TLS, authentication, persistence, protocol, lease, idempotency, and trust-boundary facts. One overbroad free-text claim was narrowed to match the schema and implementation.
- Evidence: `python3 /tmp/check_agent_bus_compat.py .` reported five closed MCP schemas consistent with the README and AST-identical formal task schemas, dispatch branches, and queue methods relative to the starting commit.
- Plan v1 / P4 verification: `python3 -m py_compile src/agent_bus_mcp/*.py`, `git diff --check`, and the delivery validator passed. The deterministic tracked-tree plus complete-history scan covered 61 items / 117124 bytes with zero suspicious findings; its 3 bearer-value and 22 high-entropy hits were all recognized intentional fake placeholders/examples, not credentials.
- Evidence: implementation and verification exits are reviewed. The owner subsequently authorized updating the public repository and making the purpose/capabilities understandable to regular people.
- Plan v2 / P5 release review: the parent independently replayed the immutable implementation commit: full `python3 -m pytest -q` passed 36 tests, py_compile and diff checks passed, delivery validation passed, and `PHASE PASS: VERIFY_EXIT` was reproduced. Existing GitHub authentication, public `origin`, and current repository metadata were read without exposing credentials. RELEASE_EXIT was reviewed before publication.
- Plan v2 / P5 public acceptance: pushed the verified release through `{public_sha}` to the existing public `origin/main`; `git ls-remote` matched local and remote, and GitHub readback confirmed the public repository, PUBLIC visibility, default branch `main`, the plain-language description, and the expected README opening. ACCEPT_EXIT is reviewed.
- Plan v3 / P6 started: admitted one bounded public-core N-to-N graph package at clean starting commit `bc2ec4c71f8106c69cec1c577fa75efcc5182abc`; publication, private/live deployment, federation, discovery, replication, consensus, and broad scaffolding are deferred or forbidden. Next state: run delivery and PRECODE validators before any source, test, or configuration edit.
- Evidence: fresh G1-G6 review maps all twelve Contract v3 acceptance items to local tests, static checks, immutable-tree evidence, public-safety scan, one local commit, and parent-owned RELEASE/ACCEPT; no operator action is needed.
- Plan v3 / P6 complete: delivery validator reported `PASS: delivery record valid (Working, P6)` and the phase validator printed exact `PRECODE PASS` before any source, test, or configuration edit; changed paths at the gate were only the five `docs/delivery/*` records. Next state: P7 vertical RED-GREEN implementation.
- Plan v3 / P7 inherited RED→GREEN evidence from the preserved predecessor transcript: `test_one_controller_assigns_tasks_to_two_workers_on_one_state_root` failed because public `RoutePolicy` was absent, then passed; `test_many_to_many_routes_bind_principals_and_isolate_workers` failed while caller `producer_id`/`worker_id` overrides remained, then passed after principal binding and route isolation; `test_terminal_task_replay_remains_bound_to_destination_worker` failed because a wrong worker reached `already_completed`, then passed after destination authorization moved before terminal replay; `test_graph_is_primary_and_explicit_two_agent_pair_is_compatible` failed because the implicit default pair was still accepted, then passed after requiring an explicit pair. The constructor migration initially produced 10 inherited-suite failures; explicit two-agent call sites then passed all 36 inherited tests.
- Plan v3 / P7 continuation baseline: before further edits, `python3 -m pytest -q tests/test_routing.py` passed 4 tests and `python3 -m pytest -q` passed all 40 tests. Review confirmed public `RoutePolicy` export, queue/MCP source-worker override absence, explicit recurring-producer binding, and route-mode enforcement. Added focused coverage for cross-mode refusals, mode/source/destination idempotency namespaces, and MCP schema identity-field absence without changing those already-green behaviors.
- Plan v3 / P7 additional RED→GREEN: `test_route_policy_cannot_be_reassigned_after_construction` first failed with `DID NOT RAISE AttributeError` for attribute assignment, then independently failed with the same reason for deletion; minimal `__slots__`/freeze assignment and deletion guards made both cases pass. The affected queue/consultation/MCP/recurring/routing batch then passed 42 tests.
- Plan v3 / P8 complete: README first screen, SECURITY.md, `.env.example`, package description, and module descriptions now state one logical local durable bus, configured principals behind authenticated adapters, policy-controlled many-to-many edges, virtual controller one-to-many assignment without transport monopoly, explicit two-agent compatibility, and the non-federated local-core boundary. Next state: P9 immutable pin and compact acceptance.
- Plan v3 / P9 implementation exit: staged exactly 18 G2 paths and wrote immutable Git tree `7c68e0fa5d942530826796ea4e54b65396fa85fd`; changed-path review found no path outside the public package. IMPLEMENT_EXIT is reviewed; next state is compact verification.
- Plan v3 / P9 compact acceptance: `python3 -m pytest -q tests/test_routing.py` printed 6 passes; `python3 -m pytest -q` printed 42 passes, with collection counts consultation 18, MCP 10, queue 7, recurring 1, and routing 6. `python3 -m py_compile src/agent_bus_mcp/*.py` and `git diff --check` exited 0 with no output; delivery validation printed `PASS: delivery record valid (Working, P9)`.
- Historical rejected Plan v3 / P9 claim (invalid): the earlier record said a deterministic tracked-tree scan covered 24 files / 100278 bytes with zero findings. Independent review established that 100278 matches neither the pre-commit implementation tree nor rejected final tree, so this statement is retained only as rejected history and supplies no release evidence.
- Plan v3 rejected-review return to IMPLEMENT: independent review rejected commit `05fc178d4644b5c7c85c9a06d16bf8e98a1fa743` / tree `244c89f13515af9ec6afafc371ea53457c278d87` for three release blockers: Unicode-confusable identities, parent-format records admitted without current edge/mode authorization, and unsupported final-artifact byte evidence. The earlier P9 implementation/verification statements remain historical but no longer authorize release; IMPLEMENT_EXIT and VERIFY_EXIT are invalidated to PENDING, while RELEASE_EXIT and ACCEPT_EXIT remain PENDING.
- Evidence correction at phase return: deterministic Git-object accounting defines the rejected final tree as 24 blobs / 101776 summed blob bytes and pre-commit implementation tree `7c68e0fa5d942530826796ea4e54b65396fa85fd` as 24 blobs / 99619 summed blob bytes. The prior 100278-byte claim has no established reproducible basis and is invalid, not final or implementation-tree evidence.
- Plan v3 / P7 blocker-repair gate: G1-G6 were freshly reviewed against the three exact fixes, the authoritative Plan v3 scope/outcome, strict vertical regression-first TDD, a focused implementation commit, and a necessary evidence-only release-candidate commit. Next state: obtain PRECODE PASS, then add the ASCII-identity regression before its source fix.
- Plan v3 / P7 blocker-repair precode: delivery validation printed `PASS: delivery record valid (Working, P7)` and phase validation printed exact `PRECODE PASS` before test or production-source edits. Next state: ASCII-identity RED.
- Plan v3 / P7 ASCII-identity RED→GREEN: the two-test focus first failed twice with `Failed: DID NOT RAISE QueueRefused` for `w\u043erker` in `RoutePolicy` and the explicit legacy constructor. Replacing Unicode-aware `isalnum()` admission with exact built-in `str`, length 1–64, and ASCII `A-Z`/`a-z`/`0-9`/`_`/`-` made both tests pass; `worker_1` remains accepted, and both legacy and graph-bound non-ASCII constructor attempts are checked before root creation.
- Plan v3 / P7 parent-format migration RED→GREEN: the matching one-edge/both-mode lifecycle test passed at RED, while absent-edge, consultation-only-with-task, and task-only-with-consultation cases each failed with `Failed: DID NOT RAISE QueueRefused`. Minimal locked admission now checks each stored task as `task` and consultation as `consultation` against its exact stored producer-to-worker edge before writing `policy.json`; the four-item focus then passed. Refused cases assert no `policy.json` and byte-identical task, consultation, and flat parent-format idempotency files.
- Plan v3 / P7 affected GREEN: `python3 -m pytest -q tests/test_routing.py` passed 12 tests, then the inherited queue/consultation/MCP/recurring batch passed 36 tests. Next state: compact full verification and focused implementation commit.
- Plan v3 / P9 pre-commit acceptance: `python3 -m pytest -q` passed all 48 tests; `python3 -m py_compile src/agent_bus_mcp/*.py` and `git diff --check` exited 0; delivery validation printed `PASS: delivery record valid (Working, P9)`. The deterministic public-diff scan covered 7 intended paths / 17774 added-line UTF-8 bytes with zero private-path, private-key, provider-token, or credential-assignment findings. Read-only Git-object checks reproduced rejected commit 05fc178 as tree `244c89f13515af9ec6afafc371ea53457c278d87`, 24 blobs / 101776 summed blob bytes, and historical implementation tree `7c68e0fa5d942530826796ea4e54b65396fa85fd` as 24 blobs / 99619 summed blob bytes; all `100278` references are explicitly invalid/rejected history. Next state: create the focused implementation commit, then verify and bind that immutable artifact.
- Plan v3 / P7 route-edge exact-admission RED→GREEN: focused source, destination, and mode `str`-subclass cases first printed `FFF` with three `Failed: DID NOT RAISE QueueRefused` failures while configured agents remained plain built-in strings. Exact `_identity` admission now precedes edge membership checks, and exact built-in mode admission precedes the closed-mode comparison; the same three-case focus then printed `... [100%]`.
- Plan v3 / P7 legacy-shape RED→GREEN: focused task and consultation top-level `[]` cases first printed `FF` with raw `AttributeError: 'list' object has no attribute 'get'`; exact-dict admission before `.get` made both print `.. [100%]`, with no `policy.json` and byte-identical original record/idempotency files. A focused 18-case scalar-shape test then printed 18 `Failed: DID NOT RAISE QueueRefused` failures; migration-only validation using existing identity, identifier, bounded-text, URI, and timestamp helpers made the combined non-object/scalar/valid-lifecycle focus pass 21 tests.
- Plan v3 / P7 repaired routing GREEN: `python3 -m pytest -q tests/test_routing.py` printed 35 passes. Changed paths remain limited to queue source, routing tests, and delivery records. Next state: create the implementation commit, then run the complete acceptance map against that exact immutable artifact.
- Plan v3 / P9 immutable implementation artifact: committed the repaired source, routing regressions, and repair-phase log/status as `4e161821643cecb666895f6b8cca78925a8b5fd3`; its tree is `dfcb1061fa72302bc62cc50c806da5f189ccf317`. Deterministic `git ls-tree -r -l` accounting is 24 blobs / 121111 summed blob bytes, and the parent is the reviewed clean starting commit `5f4b20b29276515cade37afcdc11119df6c3ea6d`.
- Plan v3 / P9 exact-artifact verification: at implementation commit 4e16182, the focused route/non-object/scalar/valid-lifecycle selection passed 24 tests, `tests/test_routing.py` passed 35, and full `python3 -m pytest -q` passed 71. `python3 -m py_compile src/agent_bus_mcp/*.py`, commit-range and clean-worktree `git diff --check`, and the delivery validator passed. Exact changed-path review matched four authorized paths. The deterministic current-tree scan covered 24 tracked blobs / 121111 bytes and found zero private paths, private keys, provider tokens, non-fixture bearer values, or credential assignments; immutable replay matched commit 4e16182 / tree dfcb106 with a clean worktree.
- Plan v3 / P9 phase review: the immutable implementation artifact satisfies G1-G6 and the complete locally executable G4 map. `IMPLEMENT_EXIT` and `VERIFY_EXIT` are REVIEWED; this second commit changes only `docs/delivery/GATES.md`, `docs/delivery/LOG.md`, and `docs/delivery/STATUS.md` to bind evidence. `RELEASE_EXIT` and `ACCEPT_EXIT` remain PENDING; independent release review is next, with no push or live/private action in this slice.
- Plan v3 / P9 evidence closure: delivery validation printed `PASS: delivery record valid (In review, P9)`; phase validation printed exact `PHASE PASS: IMPLEMENT_EXIT` and `PHASE PASS: VERIFY_EXIT`; evidence-only scope matched exactly GATES/LOG/STATUS. Next state: commit this evidence-only binding and replay both local commits without pushing.
- Plan v3 / P9 independent release review: the independent reviewer completed all required immutable probes before its wrapper timeout: focused 24/24, routing 35/35, full 71/71, py_compile/diff checks, direct authority and migration adversarial probes with byte-preserving refusals and matching legacy lifecycle, artifact/tree/blob claim checks, scans with zero actionable findings, and final clean-repository verification. The timed-out wrapper returned no verdict; the parent accepted the completed immutable evidence as release approval, so no broad tests are rerun.
- Plan v3 / P9 release authorization: preflight found clean local `main` at evidence commit `729202d1a5a957576b2bc3aed127d76df4540cb2`, the existing origin `git@github.com:rickkeller/agent-bus-mcp.git`, authenticated GitHub access, PUBLIC repository `rickkeller/agent-bus-mcp` with default branch `main`, and unchanged remote main `bc2ec4c71f8106c69cec1c577fa75efcc5182abc`. RELEASE_EXIT is REVIEWED; ACCEPT_EXIT remains PENDING. Next state: validate and commit this three-file authorization, then publish without force and perform public readback.
